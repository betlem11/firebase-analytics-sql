This is a query that extracts daily events (dated between date_start_timestamp and date_end_timestamp) from a firebase analytics table. After merging it with users and their permissions internal data brought from an external database, the events are grouped by sessions to enable user engagement calculation and other web and app analytics measures.
